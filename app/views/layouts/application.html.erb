<% use_local = eval ENV['SAMPLE_STORE_JS_LOCAL'] %>
<% use_local ||= false %>
<% js_source = use_local ? "http://localhost:3000/client_javascripts" : "https://d2b351nv4cudb7.cloudfront.net" %>
<% js_filename = Rails.env.development? ? "sample-store.dev.js" : "sample-store.herokuapp.com.js" %>

<!DOCTYPE html>
<html>
  <head>
    <title>Store</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <script src="<%= js_source %>/<%= js_filename %>"></script>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= yield %>
      <div class="clear"></div>
    </div>
  </body>
</html>
